# H题实机调试与标定

## 操作

1. 上电后 OLED 显示模式菜单，PB21 按键短按一次切换下一个模式，
   按 `1 -> 2 -> 3 -> 4 -> 5 -> 6 -> 1` 循环。
2. 持续按住 PB21 超过 1.5 秒确认并启动，启动后模式被锁定。
   运行中再次长按 1.5 秒可停止任务并解除锁定。
3. 模式 6 的指定位置由 `App/app_config.h` 中 `TASK6_TARGET_MM` 设置。

## 必做标定

1. 将车架抬离地面，确认正速度时两侧车轮均向前；方向相反时交换对应
   `AIN1/AIN2` 或 `BIN1/BIN2` 逻辑。
2. 确认循迹模块黑线为低电平。若为高电平，反转 `line_follow.c` 的判断。
   当前可直接在 `App/app_config.h` 中设置 `TRACK_ACTIVE_LOW`。若 OUT1
   实际安装在车体右侧而不是左侧，将 `TRACK_SENSOR_ORDER_REVERSED` 设为 1。
3. 调整 `LINE_BASE_SPEED_*`，再依次调整循迹 PID 的 `kp`、`kd`。
4. K210 画面中标定 `BEAM_X_MIN/MAX/Y_MIN/HEIGHT`，中心点必须输出 `X:+0`。
5. 球偏离目标后若舵机让误差继续增大，反转
   `BALANCE_SERVO_DIRECTION`。
6. 先令 `ki=0`，调大滚球 `kp` 到快速响应但不持续振荡，再增加 `kd`，
   最后只加少量 `ki` 消除静差。
7. 测量 A 到 B 的右编码器计数，写入 `AB_TARGET_ENCODER_COUNTS`。

## 模式2循迹参数

- 环路理论长度约为 `2 x 1.5 m + 2 x pi x 0.5 m = 6.14 m`。
- 20秒完成要求平均速度不低于约 `0.31 m/s`。
- `LINE_BASE_SPEED_FAST` 是直线基础输出，当前为 52%。
- `LINE_TURN_SPEED_MEDIUM/SHARP` 是进入半径0.5m弯道后的降速值。
- `MOTOR_LEFT_GAIN_PERCENT` 当前为85%，用于先补偿左轮明显偏快。
- 编码器同步控制会在循迹误差小于等于500时继续修正左右轮速差。
- 如果实测一圈超过20秒，每次将 `LINE_BASE_SPEED_FAST` 增加2，
  同时观察弯道是否丢线；不要一次大幅提高。
- 返回A点连续检测3次停车横线后使用TB6612短刹车。若停车点仍越过
  基准线，应调整传感器相对车体基准点的位置或提前停车补偿。

## 安全

- 首次测试拆下钢球并架空车轮。
- 舵机连杆安装前确认 1500 us 对应摆杆水平位置。
- K210 数据超过 100 ms 未更新时，控制器自动让摆杆回中。
- 任意时刻按 EC11 可停止电机。
