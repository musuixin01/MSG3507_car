# H 题一维摆杆滚球整合说明

## 1. 本次整合范围

题目使用的是长度 25 cm 的单凹槽摆杆，钢球只沿小车长度方向运动。因此本工程采用：

- K210 + OV2640：识别钢球中心并换算为相对摆杆中心的毫米坐标；
- UART2：以带帧头和校验的 8 字节固定帧发送坐标；
- MSPM0G3507：执行单轴积分分离 PID；
- MG996R：通过 TIMA0 50 Hz PWM 调整摆杆角度；
- OLED：显示当前坐标、目标坐标、识别和稳定状态；
- 原有 8 路循迹控制：保持原文件不变。

参考开源工程只移植了积分分离、积分限幅和执行器限幅思想。STM32F103、OV7670、LCD、USMART、二维板球任务和对应驱动没有复制到本工程。

## 2. 题目模式对应关系

| 模式 | 功能 |
| --- | --- |
| Mode 1 | 低速循迹调试，长按后松手启动 |
| Mode 2 | A 点出发，循迹一圈并在 A 点停车 |
| Mode 3 | 静止小车，钢球从 O 点到 +50 mm，再到 -50 mm 并保持 |
| Mode 4 | A 到 B 行驶，钢球目标为 O 点 |
| Mode 5 | 循迹一圈，钢球目标为 O 点 |
| Mode 6 | 启动瞬间读取钢球当前位置，并将其锁定为本圈目标位置 |

Mode 6 不需要比赛中遥控。测试人员先把钢球放到指定位置，长按并松开按键，程序在正式启动时捕获该位置。
Mode 3–6 如果启动时没有有效钢球坐标会拒绝启动，OLED 会保持 `BALL LOST`，避免小车在滚球闭环失效时驶出。

## 3. 接线

### K210 串口

| MSPM0G3507 | K210 | 说明 |
| --- | --- | --- |
| PA24 / UART2_RX | K210 UART1_TX | 必接 |
| PA23 / UART2_TX | K210 UART1_RX | 当前协议不依赖，可预留 |
| GND | GND | 必须共地 |

两端均为 115200、8 数据位、无校验、1 停止位。

### MG996R

默认使用 `PA21 / TIMA0_C0`。若舵机实际接在其他舵机口，只修改
`App/app_config.h` 中的 `BALANCE_SERVO_CHANNEL`。

MG996R 必须使用独立、稳定的舵机电源，舵机电源地和 MSPM0/K210 地必须共地。不要从单片机 3.3 V 引脚给舵机供电。

## 4. K210 首次标定

将 `K210/ball_tracking.py` 放到 K210 并运行，先通过 IDE 画面确认蓝色 ROI 完整覆盖凹槽。

依次修改：

1. `BEAM_ROI`：凹槽所在矩形区域；
2. `BEAM_NEG_120_X_PX`：刻度 -12 cm 对应的像素 X；
3. `BEAM_POS_120_X_PX`：刻度 +12 cm 对应的像素 X；
4. 如果画面坐标左右相反，将 `CAMERA_X_REVERSED` 改为 `True`；
5. 钢球漏检时调整 `CIRCLE_THRESHOLD` 和半径范围；
6. OLED 上 `X+050` 应与凹槽 +5 cm 刻度一致，允许误差应小于 5 mm。

K210 每帧都会发送数据。识别失败时发送“无球”标志，MSPM0 保持最后控制量 150 ms，随后平滑回到机械中位，避免舵机突然抽动。

## 5. MG996R 机械中位和方向

首次测试必须架空小车并防止钢球跌落。

1. 暂时保持 `BALL_PID_KI=0`、`BALL_PID_KD=0`；
2. 修改 `BALANCE_SERVO_CENTER_US`，让摆杆在静止时真正水平；
3. 把钢球放在 +3 cm 一侧，目标设为 O 点；
4. 舵机动作必须让钢球向中心滚动；
5. 如果钢球继续远离中心，只把 `BALANCE_SERVO_DIRECTION` 从 `1` 改为 `-1`；
6. 确认连杆全行程不会顶死，再调整 `BALANCE_SERVO_MIN_US` 和 `BALANCE_SERVO_MAX_US`。

默认脉宽限制为 1240–1760 us，比通用 500–2500 us 范围保守，可减少连杆撞限位风险。

## 6. PID 参数位置和调试顺序

所有滚球参数集中在 `App/app_config.h`：

- `BALL_PID_KP`：位置误差响应；
- `BALL_PID_KI`：小误差静差补偿；
- `BALL_PID_KD`：抑制冲过目标和往复摆动；
- `BALL_PID_INTEGRAL_SEPARATION_MM`：误差小于该值时才启用积分；
- `BALL_PID_OUTPUT_LIMIT_US`：PID 最大舵机修正量；
- `BALANCE_SERVO_SLEW_US_PER_TICK`：舵机指令变化速度；
- `BALL_STABLE_ERROR_MM`：稳定判定误差，目前为 8 mm。

推荐现场顺序：

1. `Ki=0, Kd=0`，从小 `Kp` 开始，使球能向目标移动；
2. 若球在目标两侧来回振荡，逐步增加 `Kd`；
3. 最后只增加少量 `Ki`，消除摩擦和机械偏心造成的静差；
4. 行驶中振荡时，优先降低 `Kp` 或增加少量 `Kd`，不要先增大 `Ki`；
5. 每次只改一个参数，并记录 Mode 3 到达 ±5 cm 的时间和最大误差。

当前参数是保守起始值。连杆长度、舵盘安装角、摆杆铰链摩擦、电源电压和摄像头帧率都会改变最终参数，烧录通过不能替代台架和赛道标定。

## 7. OLED 诊断

滚球模式下：

- `X+nnn`：当前钢球坐标，单位 mm；
- `T+nnn`：目标坐标，单位 mm；
- `BALL LOST`：串口超时、识别失败或置信度过低；
- `BALL CTRL`：正在控制；
- `BALL STABLE`：误差和速度连续满足稳定条件。

若一直显示 `BALL LOST`，先检查 K210 TX→PA24、115200 波特率、共地，再检查 K210 画面是否画出了红色圆和绿色十字。

## 8. 图传要求

K210 坐标串口只用于闭环控制，不能替代题目要求的实时图传和录像。仍需安装独立图传发送模块，并在场外使用接收与录像设备完整记录钢球运动画面。

## 9. 构建

Keil 工程：

`keil/empty_LP_MSPM0G3507_nortos_keil.uvprojx`

烧录文件：

`keil/Objects/empty_LP_MSPM0G3507_nortos_keil.hex`

本工程已经完成 SysConfig 代码生成和 Keil ARM Compiler 6 全量构建检查。实际 K210 识别、MG996R 方向、机械中位、供电和赛道表现仍必须在实物上验证。
