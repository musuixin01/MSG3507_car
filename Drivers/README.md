# Drivers

Place protocol drivers here:

- `oled/`: SSD1306 software-I2C display driver
- `mpu6050/`: MPU6050 register and attitude driver
- `communication/`: HC-05 and K210 frame parsers

Hardware pin and peripheral access should remain in `BSP/`.
